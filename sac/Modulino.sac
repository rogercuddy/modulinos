#ifndef modulino
module Modulino;
#endif

use StdIO: all;
use Array: all;
export all;

int meaning_of_life() {
  return(42);
}

#ifdef modulino
int main() {
  printf("Main: The meaning of life is %d\n", meaning_of_life());
  return(0);
}
#endif
